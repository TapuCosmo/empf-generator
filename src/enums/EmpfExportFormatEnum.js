"use strict";

/**
 * Enum for EMPF container formats.
 * @readonly
 * @enum {string}
 */
const EmpfExportFormatEnum = {
  zip: "zip",
  studioEncrypted: "studioEncrypted"
};

module.exports = EmpfExportFormatEnum;
